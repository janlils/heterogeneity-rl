"""
HTM package.

Najważniejsze moduły:
  - `codes.config`      — konfiguracja i parametry
  - `codes.market_env`  — środowisko rynku
  - `codes.algorithms`  — algorytmy i polityki
  - `codes.experiment`  — pipeline train/eval/save
  - `codes.reporting`   — wykresy i raporty
  - `codes.main`        — główny punkt wejścia CLI
"""
